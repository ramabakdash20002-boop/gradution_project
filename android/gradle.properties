# هذا الملف يحدد استهلاك الذاكرة لعملية الـ Build
# تم ضبط الإعدادات لتكون أكثر استقراراً للمشاريع التي تحتوي على ذكاء اصطناعي وكاميرا

# 💡 تم تعديل الذاكرة لـ 4 جيجا مع تفعيل نظام الـ ParallelGC لتحسين التنظيف التلقائي للرامات
# إضافة -XX:+UseParallelGC تجعل الجافا تفرغ الذاكرة غير المستخدمة فوراً أثناء التحميل
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:+UseParallelGC -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# تفعيل الذاكرة الخاصة بالـ Kotlin Daemon لضمان عدم توقف معالجة الأكواد
kotlin.daemon.jvmopts=-Xmx2G

# تفعيل دعم المكتبات الحديثة والقديمة معاً
android.useAndroidX=true
android.enableJetifier=true

# تحسين سرعة واستقرار التجميع
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

# تقليل استهلاك الموارد عند عدم الحاجة لسرعة الاستجابة
org.gradle.configureondemand=true