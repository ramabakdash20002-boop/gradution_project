name: gradution_project
description: "A new Flutter project for Blind Assistant using Google ML Kit."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 💡 المحرك الجديد: Google ML Kit (أكثر استقراراً واحترافية)
  google_mlkit_image_labeling: ^0.14.1
  
  # 💡 مكتبات الكاميرا والصوت
  camera: ^0.11.0         # لإدارة البث الحي للكاميرا
  flutter_tts: ^4.2.3     # نطق النصوص للمكفوفين
  path_provider: ^2.1.1
  permission_handler: ^12.0.1

  # 💡 مكتبات Firebase (تم توحيدها لضمان التوافق)
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1

  # 💡 المساعد الصوتي والاهتزاز
  speech_to_text: ^7.3.0  # تحويل صوت الكفيف لأوامر
  vibration: ^3.1.4       # تنبيهات بالاهتزاز عند وجود خطر

  # 💡 مكتبات مساعدة
  image_picker: ^1.0.4
  http: ^1.1.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # 💡 تعريف الملفات بناءً على هيكل المجلدات الفعلي لديكِ
  assets:
    # الصور المستخدمة في شاشات الفئات داخل مجلد lib
    - lib/assets/eldry.png
    - lib/assets/student.png
    - lib/assets/child.png
    # تسجيل مجلد assets الرئيسي لأي ملفات إضافية
    - assets/