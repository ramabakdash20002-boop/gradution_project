name: gradution_project
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  # 💡 حزم Firebase الضرورية (تم توحيد الإصدارات لضمان التوافق)
  firebase_core: ^4.2.1  # تم التحديث لأحدث إصدار متوافق
  firebase_auth: ^6.1.2  # تم التحديث ليتوافق مع Core
  cloud_firestore: ^6.1.0 # هذه الحزمة ممتازة وموجودة

  # مكتبات الميزات الإضافية
  image_picker: ^1.0.4
  flutter_tts: ^4.2.3
  speech_to_text: ^7.3.0
  vibration: ^3.1.4
  http: ^1.1.0 
  
  # مكتبة الأيقونات (تم حذف التكرار)
  cupertino_icons: ^1.0.2 

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^6.0.0 # تم التحديث لأحدث إصدار للـ Lints لتجنب التحذيرات

flutter:
  uses-material-design: true

  # مسار الأصول (تأكدي أن المسافات صحيحة)
  assets:
    - lib/assets/
    